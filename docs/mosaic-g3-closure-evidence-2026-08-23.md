# Mosaic G3 Closure Evidence — 2026-08-23

## Status And Scope

G3 is complete on the exact first-party application graph below. The package-owned runtime reaches one exact contributor host commit through OpalBase's sole Wallet-facing facade; Wallet persists the role-specific post-manifest archive and exact admission, publication, and terminal companion journals; one supervised application session owner reconstructs terminal state through a fresh persistence, codec, repository, and owner composition; and recovery reaches explicit broadcast approval with zero relay opens, zero relay sends, and no persisted broadcast intent.

This evidence closes only the private application-session composition gate. Production Mosaic composition remains structurally disabled. No external relay, Tor daemon, Fulcrum process, BCH node, transaction broadcaster, value-moving path, public Mosaic session, tag, release, or `main` promotion was used or enabled.

## Exact First-Party Graph

| Repository | Exact revision | G3 role |
| --- | --- | --- |
| Wallet | `f0d31c6adead55ad405ccf45f8d77d08d98a2e85` | Promoted G3 application composition and dated closure evidence; implementation is introduced by `e5021d9d68a85d2f33ab6bcb7bcc2c151b4a01f5` |
| OpalBase | `35709a24fa3122c55181d93c79745a7018d1fc31` | Sole Wallet-facing facade over bootstrap, formation, role projection, exact host execution, journal recovery, and terminal disposition |
| OpalFusion | `79ba5f91449b2c0d5cd6ec73c38fafd58cae0b46` | Package-owned formation and execution semantics plus route-free terminal companion-journal replay |
| OpalCrypto | `23425db04075a48405d65cf6a3e2254911e626eb` | First-party hashing, BIP340, NIP-44, RSABSSA, key derivation, authenticated encryption, and key validation |
| SwiftFulcrum | `24b44bb2458822d14121dfbd57321fda7ae539ea` | Existing chain dependency; G3 constructed no chain client and contacted no network |
| OpalDiagnostics | `7cd2e383309821e01903077c1e534174f9c8a964` | Existing first-party diagnostics foundation |

Wallet's application and test targets directly depend only on OpalBase, OpalCrypto, and OpalDiagnostics. OpalFusion remains an OpalBase implementation dependency. The resolved package revision, checkout `HEAD`, and remote `develop` revision matched for all five package repositories before compilation. No external production cryptographic calculation dependency was introduced.

## Package Evidence

OpalFusion `79ba5f91449b2c0d5cd6ec73c38fafd58cae0b46` makes terminal execution restoration independent of live relay routes while retaining authenticated admission, publication, and terminal companion-journal evidence. Its package build passed, the focused recovery suite passed seven tests in 11.168 seconds, and the real-cryptography terminal recovery case passed in 199.888 seconds. Class D pre-promotion and post-promotion gates passed; private `draft`, local `develop`, and public `develop` aligned at the exact revision while public `main` remained unchanged.

OpalBase `35709a24fa3122c55181d93c79745a7018d1fc31` projects package-owned formation and post-manifest contracts through Base-owned SPI values. Wallet has no OpalFusion import, linked product, direct package declaration, or second runtime constructor.

## Application Evidence

Wallet's format-v3 authenticated outer record carries the contributor or conductor post-manifest archive, 23 exact authorization-recovery state pairs, purpose-separated conductor authorization-key references, and synchronous admission, publication, and terminal journal snapshots under the existing single persistence owner. The same `MosaicPrivateAlphaSessionOwnerActor` owns fresh and recovered execution.

The deterministic producer fixture was generated once from the exact OpalFusion revision. Its canonical JSON is 1,705,322 bytes with SHA-256 `ec6755c6cf6a736ddf8538d32e0fa48afc247342fe6a4a424d826a0a62f2ee7a`; the committed LZFSE/base64 Wallet resource is 1,542,168 bytes. It pins phase counts `[2, 48, 3, 2, 4]`, 78 outbound publications, 23 authorization-recovery states, eight previous transactions, and contributor index zero.

The exact Wallet package graph resolved in one shared source-package lane, the remote-aware dependency doctor passed all five packages, and headless macOS Debug `build-for-testing` completed with `TEST BUILD SUCCEEDED`. Xcode test enumeration selected each case by its generated identifier before execution; no selector fallback or full-suite retry ran.

- The fixture authentication and decode validator passed one test in 0.007 seconds.
- The route-loss validator passed one test in 0.114 seconds. All three route allocations terminalized with credentials erased; restart resumed exact publication state; authenticated abort then reached exact physical cleanup.
- The single serialized real-RSABSSA application rehearsal passed one test in 173.145 seconds. Its internal formation-stage ceiling remained under 15 seconds. It delivered five phase groups and all 78 unique publications, including one player commit, 23 anonymous component publications, the expected pre-sign acknowledgements, and one BCH signature, then reached exact terminal host commit without broadcast.
- A fresh persistence actor, record codec, repository, and session owner loaded the terminal record and exact admission, publication, and terminal journals. Recovery returned broadcast approval, preserved the terminal journal byte-for-byte, retained no broadcast intent, and recorded zero relay opens and zero relay sends.

The fast validator's first invocation did not reach the test because the filesystem sandbox denied Xcode access to its SwiftPM diagnostics cache. Repeating the same already-built selector outside that sandbox boundary passed. This was an execution-environment correction rather than a failing-test retry.

## Environment And Commands

Validation ran on an arm64 Mac with macOS 27.0 build 26A5416b, Xcode 27.0 build 27A5237l, and Apple Swift 6.4 build `swiftlang-6.4.0.30.4`. Xcode used repository-owned `.codex-cache/xcode-worktree-control/DerivedData` and `.codex-cache/xcode-worktree-control/SourcePackages` paths, exact `Package.resolved` revisions, disabled automatic package resolution, disabled signing for the headless macOS test build, and no parallel test workers.

The application lane used `build-for-testing`, `-enumerate-tests`, and then `test-without-building` with only the exact fixture, route-loss, or real-RSABSSA identifier. The real-RSABSSA case had retries disabled and a 600-second maximum test allowance. No full application suite, external network, or value-moving lane ran.

## Disable And Recovery Procedure

If later evidence invalidates G3, keep production Mosaic composition disabled, deny every new route allocation and session start, preserve authenticated recovery or quarantine evidence, suspend the sole session owner, and recover only through the exact graph and package-owned terminal semantics. Never infer broadcast authority from terminal host commit. Reopen the earliest affected gate, correct the owning repository first, promote its validated `draft` to `develop`, repin the consumer graph, and repeat the bounded G3 evidence before resuming later-gate work.

## Residual Risks And Non-Proofs

G3 does not prove clean Release/archive readiness, deterministic repository CI, complete golden vectors, parser fuzz coverage, multi-device reliability, external Tor behavior, relay-operator or circuit independence, timing privacy, traffic-analysis resistance, independent review, chain reconciliation, confirmation or reorganization handling, value accounting, a bounded canary, residual-risk acceptance, public readiness, production readiness, audit status, or anonymity. Those remain G4 through G6 responsibilities. Independent review, external-environment evidence, isolated live chain access, transaction broadcast, value movement, canary execution, residual-risk acceptance, public-lane promotion, tags, releases, and publication remain separately authorized or non-self-certifying boundaries.
