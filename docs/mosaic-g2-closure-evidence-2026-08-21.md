# Mosaic G2 Closure Evidence — 2026-08-21

Status: G2 is complete at the exact first-party graph below. OpalFusion owns the authenticated bootstrap and post-manifest transport semantics, OpalBase is the sole Wallet-facing facade, and Wallet owns authenticated transport state, application keys, relay policy, and the concrete Tor-only adapter. Production composition remains disabled, Wallet has no direct OpalFusion dependency, and no external relay, Tor daemon, chain service, broadcaster, wallet secret, or value-moving path was used.

## Exact Graph And Ownership

| Repository | Exact revision | G2 responsibility |
| --- | --- | --- |
| Wallet | `ca15e50e0db858e64298a680dcdb51438edfbf38` | Final G2 evidence over application implementation `5da66b238aef3db99119eced43d8a2c547af4a29`: authenticated v2 state, persistent authorization identity, monotonic transport facts, reviewed relay-policy input, concrete Tor-only WebSocket routes, production-adapter loopback, and physical cleanup proof |
| OpalBase | `54ad1d1606226a6afa63ce89cb9ff02fcc46f4e3` | Wallet-facing transport contracts and the sole adapter into OpalFusion |
| OpalFusion | `117949b08f354f5a58b78075dbf14b2ec4e4f88f` | Bootstrap protocol, authenticated registration consensus, exact wrapper retry, replay-restored fan-in, and reconnect semantics |
| OpalCrypto | `23425db04075a48405d65cf6a3e2254911e626eb` | RSA `SecKey` validation, RFC 9474 recovery, NIP-44, BIP-340, SHA-256, and secure random calculation |
| SwiftFulcrum | `24b44bb2458822d14121dfbd57321fda7ae539ea` | Existing chain dependency only; G2 does not construct or contact it |
| OpalDiagnostics | `7cd2e383309821e01903077c1e534174f9c8a964` | Existing first-party diagnostics foundation; G2 adds no secret-bearing logging |

Wallet's Xcode targets directly depend only on OpalBase, OpalCrypto, and OpalDiagnostics. OpalFusion is resolved transitively because OpalBase owns the implementation adapter; Wallet's project file contains no OpalFusion package reference, target dependency, import, or qualified runtime use. The transitive package row visible in Xcode is expected resolved state and is not a duplicated application dependency.

No external production cryptographic calculation dependency was added. OpalCrypto remains the calculation owner, and every workflow or protocol decision stays in its purpose-specific first-party repository.

## Closure Evidence

The package producer evidence in [Mosaic G2 Package Evidence — 2026-08-21](mosaic-g2-package-evidence-2026-08-21.md) records the authenticated mailbox-distribution protocol, complete-roster consensus, exact-three-source fan-in, durable publication recovery, cancellation-safe route cleanup, and recovery-safe proof boundary. On the final G2 producer, the RSA-free `mosaic-fast` lane passed 6/6 tests, the generation-free transport lane passed 5/5 tests, the non-`@testable` consumer surface passed 4/4 tests, and the package build, dependency parity, public-boundary checks, and Class D promotion checks passed.

Wallet's repository-owned evidence is `docs/readiness/mosaic-g2-application-evidence-2026-08-21.md` at `ca15e50e0db858e64298a680dcdb51438edfbf38`. Its final focused macOS matrix passed 47 logical tests and 56 executed cases with zero failures, skips, expected failures, or runtime warnings in 15.897 seconds. The concrete production-adapter SOCKS5/TLS loopback completed in 0.040 seconds and proved three independently authenticated routes used SOCKS domain-address requests instead of local target-host DNS.

The standalone signed macOS fresh-process lane passed two tests in one suite in 6.926 seconds, exercising the production file and Keychain clients across the inherited G1 crash boundaries and cross-process exclusion. The generic iOS Wallet build passed with the macOS-only private transport excluded. Role-first structure, intent/capability boundaries, plan metadata, whitespace, direct-OpalFusion source scans, and Xcode target-reference scans passed. The remote-aware dependency doctor reported `pass` for all five resolved dependencies, including transitive OpalFusion at the exact revision above.

## Security And Failure Posture

- Format-v2 outer state authenticates the exact graph, persistent RSA verification identity, immutable relay binding and key material, monotonic blind-request and registration facts, publication acknowledgements, replay facts, and route allocations while format v1 retains its exact G1 behavior.
- Route credentials are generated by OpalCrypto, persisted before connection construction, uniquely bound to one allocation, and erased when the reservation closes, fails, cancels, loses its source, or is recovered after restart.
- Production transport is disabled by default and has no environment override or compiled endpoint registry. An enabled configuration requires exactly three canonical WSS endpoints with distinct reviewed operator labels, rejects NIP-42 and proof-of-work requirements, and permits only a numeric-loopback SOCKS5 proxy.
- The concrete adapter uses an ephemeral session, route-specific SOCKS credentials, no failover, ordinary Web PKI, no redirects, bounded serialized I/O, cancellation joining, and no clearnet constructor. Its custom trust path exists only in `DEBUG` for the local harness.
- Missing, extra, duplicate, malformed, stale, replaced, or contradictory record, anchor, key, route, acknowledgement, wrapper, or replay evidence fails closed. Cleanup proves absence of the envelope, root key, salt, authorization key, and raw route credentials before terminal deletion is accepted.

## Approval, Disable, And Non-Proofs

The repository owner authorized direct cross-repository work, commits, pushes, and validated `draft -> develop` integration-candidate promotion for this Mosaic goal. G2 used only local deterministic and loopback validation. No external-network smoke, endpoint credential, relay publication, Tor daemon, Fulcrum connection, broadcast, value movement, canary, `main` promotion, tag, release, or public enablement was authorized or performed.

Disable remains structural: Wallet's production transport configuration is disabled and has no production endpoint registry, while no G3 session owner invokes the transport. If this evidence is invalidated, keep the configuration disabled, preserve authenticated or quarantined records, stop and join route owners, terminalize credential reservations, and rerun the exact producer, facade, dependency, persistence, and loopback gates before proceeding.

This evidence does not prove an external relay, Tor circuit independence, durable third-party delivery, traffic-analysis resistance, anonymity, a supervised application session, exact host commit, chain reconciliation, broadcast safety, value accounting, canary readiness, release readiness, independent review, or G3 through G6 closure.
